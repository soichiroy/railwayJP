% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{plot_railway_segments}
\alias{plot_railway_segments}
\title{Plot railway segments on a map}
\usage{
plot_railway_segments(
  segments,
  output_file = "railway_map.png",
  title = "Railway Routes",
  subtitle = NULL,
  show_all_stations = TRUE,
  key_stations = NULL,
  show_legend = TRUE,
  width = 14,
  height = 10,
  dpi = 150,
  padding_x = 0.5,
  padding_y = 0.3
)
}
\arguments{
\item{segments}{A list of route specifications. Each element should be a list with:
\describe{
\item{start}{Start station name (required)}
\item{end}{End station name (required)}
\item{via}{Character vector of intermediate station names (optional)}
\item{line}{Line name(s) to filter by (optional). Can be a character vector
for routes spanning multiple lines.}
\item{operator}{Operator name(s) to filter by (optional). Can be a character
vector for routes spanning multiple operators
(e.g., \code{c("四国旅客鉄道", "西日本旅客鉄道")}).}
\item{color}{Custom color for this route (optional, uses operator color if available)}
\item{label}{Label for legend (optional)}
}}

\item{output_file}{Output filename (default: "railway_map.png")}

\item{title}{Plot title}

\item{subtitle}{Plot subtitle (if NULL, auto-generated from routes)}

\item{show_all_stations}{If TRUE, show all stations along routes}

\item{key_stations}{Character vector of station names to highlight with labels}

\item{show_legend}{If TRUE, show route legend}

\item{width}{Plot width in inches}

\item{height}{Plot height in inches}

\item{dpi}{Output resolution}

\item{padding_x}{Horizontal padding around routes (in degrees)}

\item{padding_y}{Vertical padding around routes (in degrees)}
}
\value{
A ggplot2 object (invisibly)
}
\description{
Creates a map showing one or more railway routes across Japan. Each segment
can specify start/end stations, intermediate stops (via points), and optional
filtering by line name or operator.
}
\examples{
\dontrun{
# Single route
plot_railway_segments(
  segments = list(
    list(start = "東京", end = "横浜", label = "東海道線")
  ),
  output_file = "tokyo_yokohama.png"
)

# Multiple routes with via points
plot_railway_segments(
  segments = list(
    list(
      start = "新宿",
      end = "長野",
      via = c("松本"),
      operator = "東日本旅客鉄道",
      label = "中央線"
    ),
    list(
      start = "東京",
      end = "大阪",
      via = c("名古屋"),
      label = "東海道新幹線"
    )
  ),
  title = "Railway Routes",
  key_stations = c("新宿", "松本", "長野", "東京", "名古屋", "大阪")
)
}
}
